package ${package};

import org.springframework.stereotype.Service;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import java.util.List;
import javax.annotation.Resource;

/**
 * ${comments}服务层实现类
 *
 * @version v0.1, ${nowTime}, create by ${author}.
 */
@Service
public class ${upperClassName}ServiceImpl extends BaseDaoImpl<${upperClassName}PO> implements I${upperClassName}Service {

    /**
     * 日志
     */
    private static final Logger LOGGER = LoggerFactory.getLogger(${upperClassName}ServiceImpl.class);


}
