package ${package};

import com.baomidou.mybatisplus.extension.service.IService;
import ${package}.${className}DO;

/**
 * ${comments}服务层接口
 *
 * @version v0.1, ${datetime}, create by ${author}.
 */
public interface I${className}Service {

    /**
     * 分页查询${comments}
     *
     * @param ${classname}
     *              查询条件对象
     * @param pageNum
     *              当前第几页
     * @param pageSize
     *              每页条数
     * @return 分页结果
     */
    Page<${className}DO> queryPages(${className}DO ${classname}, Integer pageNum, Integer pageSize);

    /**
     * 新增${comments}
     *
     * @param ${classname}
     *              新增对象信息
     * @return 影响的行数
     */
    int save(${className}DO ${classname});

    /**
     * 通过主键修改${comments}
     *
     * @param ${pk.attrname}
     *                  主键
     * @param ${classname}
     *                  要修改的信息
     * @return 影响的行数
     */
    int updateById(${pk.attrType} ${pk.attrName}, ${className}DO ${classname});

    /**
     * 通过主键删除${comments}
     *
     * @param ${pk.attrName}
     *              主键
     * @return 影响的行数
     */
    int deleteById(${pk.attrType} ${pk.attrName})

}
